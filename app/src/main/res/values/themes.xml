<resources>
    <!-- Base application theme. -->
    <style name="Theme.HabitTracker" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_03</item>
        <item name="colorPrimaryVariant">@color/blue_02</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_02</item>
        <item name="colorSecondaryVariant">@color/blue_04</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- Customize your theme here. -->
        <item name="bottomSheetDialogTheme">@style/Theme.HabitTracker.BottomSheetDialog</item>
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <style name="Theme.HabitTracker.Main" parent="Theme.HabitTracker">
        <item name="android:windowBackground">@color/blue_01</item>
    </style>

    <style name="Theme.HabitTracker.Edit" parent="Theme.HabitTracker">
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Theme.HabitTracker.BottomSheetDialog" parent="Theme.Material3.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>
</resources>